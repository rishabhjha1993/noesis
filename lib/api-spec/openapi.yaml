openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: analysis
    description: Visual analysis operations
paths:
  /analyze:
    post:
      operationId: analyzeImage
      tags: [analysis]
      summary: Analyze an uploaded visual
      description: Runs AI vision analysis on an uploaded image and returns a guided walkthrough payload
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnalysisInput"
      responses:
        "200":
          description: Analysis result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoesisAnalysis"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorMessage"
        "502":
          description: Analysis failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiErrorMessage"
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    ApiErrorMessage:
      type: object
      properties:
        error:
          type: string
      required:
        - error
    AnalysisInput:
      type: object
      properties:
        image_data_url:
          type: string
          description: Data URL (base64) of the uploaded image, e.g. data:image/png;base64,...
          minLength: 30
      required:
        - image_data_url
    NoesisRegion:
      type: object
      properties:
        id: { type: string }
        label: { type: string }
        x: { type: number, minimum: 0, maximum: 1 }
        y: { type: number, minimum: 0, maximum: 1 }
        width: { type: number, minimum: 0, maximum: 1 }
        height: { type: number, minimum: 0, maximum: 1 }
        sequence_order: { type: number }
        explanation: { type: string }
        why_it_matters: { type: string }
        related_region_ids:
          type: array
          items: { type: string }
        relationship_explanation: { type: string }
        confidence: { type: number, minimum: 0, maximum: 1 }
      required:
        - id
        - label
        - x
        - y
        - width
        - height
        - sequence_order
        - explanation
        - why_it_matters
        - related_region_ids
        - relationship_explanation
        - confidence
    NoesisAnalysis:
      type: object
      properties:
        title: { type: string }
        image_type: { type: string }
        central_question: { type: string }
        overall_summary: { type: string }
        big_takeaway: { type: string }
        regions:
          type: array
          items:
            $ref: "#/components/schemas/NoesisRegion"
      required:
        - title
        - image_type
        - central_question
        - overall_summary
        - big_takeaway
        - regions
